// Concat com strings
const filhas = ['Valesca', 'Michele']
const filhos = ['Marcos', 'José']
const todosFilhos = filhas.concat(filhos, 'Bastardo kkk')
console.log('Filhos: ', todosFilhos)

// Array vazio + array numerico + array numerico + matriz numerica
const outroExemplo = [].concat([1, 2, 3], [4, 5, 6], [[6, 7]])
console.log('Outro exemplo: ', outroExemplo)


/**
 * Concat é usado pricipalmente para concatenar arrays
 * 
 */